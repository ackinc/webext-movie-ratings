info:
  name: Delete all docs in index
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{MEILISEARCH_URL}}/indexes/{{indexName}}/documents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
