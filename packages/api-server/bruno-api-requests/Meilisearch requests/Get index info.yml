info:
  name: Get index info
  type: http
  seq: 2

http:
  method: GET
  url: "{{MEILISEARCH_URL}}/indexes/:indexName"
  params:
    - name: indexName
      value: "{{indexName}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
