info:
  name: Search index
  type: http
  seq: 6

http:
  method: GET
  url: "{{MEILISEARCH_URL}}/indexes/{{indexName}}/search?limit=5&q=Commando Mogambo&showRankingScore=true"
  params:
    - name: limit
      value: "5"
      type: query
    - name: q
      value: Commando Mogambo
      type: query
    - name: showRankingScore
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
