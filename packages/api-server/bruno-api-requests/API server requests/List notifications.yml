info:
  name: List notifications
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/notifications?from=1782982754693"
  params:
    - name: from
      value: "1782982754693"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
