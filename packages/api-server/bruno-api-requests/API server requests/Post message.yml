info:
  name: Post message
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/messages"
  body:
    type: json
    data: |-
      {
        "email": "anirudh.nimmagadda@gmail.com",
        "category": "other",
        "message": "Hello2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
