info:
  name: With placeholder
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/notifications"
  body:
    type: json
    data: |-
      {
        "notificationId": "TEST_NOTIFICATION_WITH_PLACEHOLDER",
        "targetPage": "filters",
        "content": "This is a test notification with a {{placeholderName}}!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
